export type Education = {
    title: string;         // Название учебного заведения
    institution: string;   // Специальность / направление
    year: string;          // Год или период обучения (в формате строки)
  };
  
  export type Experience = {
    title: string;         // Название компании / организации
    company: string;       // Должность
    period: string;        // Период работы (в формате строки)
  };
  